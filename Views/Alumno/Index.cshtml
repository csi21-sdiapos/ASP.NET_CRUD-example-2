@model IEnumerable<ASP.NET_CRUD_example_2.Models.AlumnoDTO>

@{
    ViewData["Title"] = "Index";
}

<h1>Index</h1>

<p>
    <a asp-action="Create">Create New</a>
</p>
<table class="table">
    <thead>
        <tr>
            <th>
                @Html.DisplayNameFor(model => model.Alumno_nombre)
            </th>
            <th>
                @Html.DisplayNameFor(model => model.Alumno_apellidos)
            </th>
            <th>
                @Html.DisplayNameFor(model => model.Alumno_email)
            </th>
            <th></th>
        </tr>
    </thead>
    <tbody>
@foreach (var item in Model) {
        <tr>
            <td>
                @Html.DisplayFor(modelItem => item.Alumno_nombre)
            </td>
            <td>
                @Html.DisplayFor(modelItem => item.Alumno_apellidos)
            </td>
            <td>
                @Html.DisplayFor(modelItem => item.Alumno_email)
            </td>
            <td>
                <a asp-action="Edit" asp-route-id="@item.Alumno_id">Edit</a> |
                <a asp-action="Details" asp-route-id="@item.Alumno_id">Details</a> |
                <a asp-action="Delete" asp-route-id="@item.Alumno_id">Delete</a>
            </td>
        </tr>
}
    </tbody>
</table>
