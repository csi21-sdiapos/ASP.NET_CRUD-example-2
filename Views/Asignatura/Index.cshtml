@model IEnumerable<ASP.NET_CRUD_example_2.Models.AsignaturaDTO>

@{
    ViewData["Title"] = "Index";
}

<h1>Index</h1>

<p>
    <a asp-action="Create">Create New</a>
</p>
<table class="table">
    <thead>
        <tr>
            <th>
                @Html.DisplayNameFor(model => model.Asignatura_nombre)
            </th>
            <th></th>
        </tr>
    </thead>
    <tbody>
@foreach (var item in Model) {
        <tr>
            <td>
                @Html.DisplayFor(modelItem => item.Asignatura_nombre)
            </td>
            <td>
                <a asp-action="Edit" asp-route-id="@item.Asignatura_id">Edit</a> |
                <a asp-action="Details" asp-route-id="@item.Asignatura_id">Details</a> |
                <a asp-action="Delete" asp-route-id="@item.Asignatura_id">Delete</a>
            </td>
        </tr>
}
    </tbody>
</table>
